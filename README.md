node-addon-unordered-map
========================

This project demonstrates a bug when using `std::unordered_map` in a Node addon.

Usage
-----

```
npm install
npm test
```

Results
-------

See CircleCI for results across a number of Node × compiler versions.
